/**
 * §8.2：谬误面板（右侧抽屉），confidence 三档渲染：<0.6 灰显。
 * 区分「渲染分档」与 config.fallacy_alert_at（是否出红色）两件事，别混用（§8.2 注）。
 * 归属 P4。
 */
export default function FallacyPanel() {
  // 骨架：契约见 §8.2，实现随 §13.2 对应期
  return null;
}
