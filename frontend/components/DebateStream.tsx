/**
 * §8.2：对话流；订阅 §6.12 SSE 事件（turn.started / message.delta / message.layer / turn.done）。
 * 归属 P1（§13.2 P1 前端交付物）。
 */
export default function DebateStream() {
  // 骨架：契约见 §8.2，实现随 §13.2 对应期
  return null;
}
