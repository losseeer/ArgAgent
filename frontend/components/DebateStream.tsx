/**
 * 骨架占位文件：只声明职责与契约，尚无实现。
 * 对话流：订阅后端 SSE 事件（turn.started / message.delta / message.layer / turn.done）。
 * 落地顺序上排最先：真实流式渲染先于台账、证据、谬误三个面板。
 */
export default function DebateStream() {
  // 骨架占位：尚无实现
  return null;
}
