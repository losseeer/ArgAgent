/**
 * §8.2：五类降级/提示角标的唯一挂载点。
 * N8 断言的就是「角标是否存在且与真实状态一致」——所以本组件必须是唯一出口（§10.2 N8）。
 * 空 key 启动时必须能看到「审核降级」角标（§13.2 P1 验收）。
 */
export default function BadgeBar() {
  // 骨架：契约见 §8.2，实现随 §13.2 对应期
  return null;
}
