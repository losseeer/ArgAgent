/**
 * 骨架占位文件：只声明职责与契约，尚无实现。
 * 五类降级/提示角标的唯一挂载点，本组件必须是角标的唯一出口。
 * 可见性口径：角标必须与真实状态一致，缺角标即视为缺陷（对应评测里的审核降级可见性一项，断言尚未进 CI）。
 * 未配置审核 key 启动时，必须能看到「审核降级」角标。
 */
export default function BadgeBar() {
  // 骨架占位：尚无实现
  return null;
}
