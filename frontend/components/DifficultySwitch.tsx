/**
 * casual / competitive / brutal；会话内可改，自下一回合生效（Q6 已决）。
 * PUT /api/sessions/{sid}/config 只接受 difficulty 与 strictness 两个键，其余键 422。
 */
export default function DifficultySwitch() {
  // 骨架：契约见 §8.2，实现随 §13.2 对应期
  return null;
}
