# 评测报告（`make eval` 的产出，模板即本文件的结构）

> 本文件由 `backend/app/eval/runner.py` 覆盖生成，勿手改结论。
> 任一红线未过则 README badge 转红；样本量决定能下什么结论——本报告只写样本量撑得住的话。

## 结果总表

| 族 | 指标 | 阈值（以本表为准） | 本次值 | ±CI | 与上一版成对差 | 结论 |
|---|---|---|---|---|---|---|
| 结构 | 层标签合规 / 引用真实性 / 未核查标记 / 重试降级分布 / 预算遵守 / 台账不变量 / 词表零污染 / 审核降级可见 | 零 LLM 成本，CI 常驻 | | n/a | | |
| 一致性 | 矛盾剧本召回 / 诱饵误报 | ≥27/30、0 误报 | | | | |
| gold 集质量 | 谬误标注 precision / 检索触发率 | ≥85%；漏触发 ≤20% | | | | |
| 鲁棒 | 受控改写翻转率（judge 一致性）/ 注入蜜标执行率 | 翻转率达标才允许启用 LLM-as-Judge 辅助层；注入执行率 = 0 | | | | |
| 时延 | 首字 P95 | ≤6s（≥12s 即 fail） | | | | |

## 明确不评测（写死，防范围蔓延）

人类评委盲评、说服力/深度打分、跨辩题泛化（只承诺内置 10 题）、教育效果主张（不做，
也不写"效果未验证"这类反向暗示）、agent-loop 模式的一切指标（不进任何门禁、不抽样，
只在界面留「无护栏」标注）。

## 已知上限（必须随报告一起披露）

- 矛盾检测只覆盖 `predicate_vocab.jsonl` 内断言；词表外句子不参与判定，也不会出现在矛盾卡里。
- 启用检索只标注来源与 `tier`，不担保核查完整。
- 本项目的贡献是集成 + 一致性约束 + 领域自建评测，非新算法。
