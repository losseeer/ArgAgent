# 评测报告（`make eval` 的产出，模板见 §10.6）

> 本文件由 `backend/app/eval/runner.py` 覆盖生成，勿手改结论。
> 任一红线未过则 README badge 转红；样本量与 ±置信区间的边界见 §10.5——本报告只写样本量撑得住的话。

## 结果总表

| 族 | 指标 | 阈值（§10.2 唯一出处） | 本次值 | ±CI | 与上一版成对差 | 结论 |
|---|---|---|---|---|---|---|
| N | N1–N8 结构断言 | 零 LLM 成本，CI 常驻 | | n/a | | |
| K | K1 矛盾召回 / K2 诱饵误报 | ≥27/30、0 误报 | | | | |
| V | V1 谬误 precision / V4 触发 | ≥85%；漏触发 ≤20% | | | | |
| R | R1 judge 一致性 / R3 注入执行率 | R1 达标才允许 L2-J；R3 = 0 | | | | |
| O | O1 首字 P95 | ≤6s（≥12s 即 fail） | | | | |

## 明确不评测（§10.6，写死）

人类评委盲评、说服力/深度打分、跨辩题泛化（只承诺内置 10 题）、教育效果主张（Q3 已决：不做）、
agent-loop 模式的一切指标（Q8 已决：不进门禁、不抽样，只留 UI「无护栏」标注）。

## 已知上限（必须随报告一起披露，§9.4 第 1/2/4 条）

- 矛盾检测只覆盖 `predicate_vocab.jsonl` 内断言；词表外句子不参与判定，也不会出现在矛盾卡里。
- 启用检索只标注来源与 `tier`，不担保核查完整。
- 本项目的贡献是集成 + 一致性约束 + 领域自建评测，非新算法。
