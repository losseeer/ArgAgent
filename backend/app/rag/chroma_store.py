"""骨架文件：只声明归属与契约指针，实现见 §13.2 对应期。

 - §7.5 向量层，本地 ./data/chroma。只有 topic_definitions / fallacy_examples / walton_schemes 三个 jsonl 进 Chroma；predicate_vocab、source_tiers、safety_patterns 是精确匹配表，**不进 Chroma**（§4 树注）。
 - ORCHID 中文辩论语料不入表（Q4 已决 · 第十七轮；恢复条件见 §2.3 该行）。
"""
