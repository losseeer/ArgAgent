"""骨架占位文件：只声明职责与契约，尚无实现。

 - match() / unify() / merged_into 重定向的**唯一实现点**，
   用户侧（每轮起草前比对历史断言）与 agent 侧（出口校验时比对自身断言）共用。
 - 判定走谓词化：原"余弦相似度 + 立场倾向相反"的 embedding 方案已废弃
   （反义句在向量空间里高度相似，是已知失效模式）。
 - 只加载 status='accepted' 的词表条目；喂 candidate 条目应直接报错。
 - 属记忆与一致性能力，尚未实现（骨架占位）。
"""
