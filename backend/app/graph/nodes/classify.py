"""骨架文件：只声明归属与契约指针，实现见 §13.2 对应期。

 - §6.2 节点契约：意图分类 + 三层断言（[FACT]/[ANALOGY]/[LOGIC]）+ 谓词抽取，
   **同一次结构化调用**产出（§7.3）。
 - 输出 schema 一次到位含 predicates，词表未就绪时值可为空——别让 P2 再改出口契约（§13.2 P1）。
 - T0：连整句断言写 claims(role='user')，返回 user_claim_ids（§6.11 时序表）。
 - 词表外句子必须返回空 pred_id，且容错为 0（Q19 已决 · 第十九轮，口径唯一出处 §10.2 N7）。
"""
