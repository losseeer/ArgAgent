"""骨架占位文件：只声明职责与契约，尚无实现。

 - 节点契约：意图分类 + 三层断言（[FACT]/[ANALOGY]/[LOGIC]）+ 谓词抽取，
   **同一次结构化调用**产出。
 - 输出 schema 一次到位含 predicates，词表未就绪时值可为空——后续不再改出口契约。
 - 用户消息入库时：连整句断言写入 claims(role='user')，返回 user_claim_ids。
 - 词表外句子必须返回空 pred_id，且容错为 0。
"""
