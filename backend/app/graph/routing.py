"""骨架文件：只声明归属与契约指针，实现见 §13.2 对应期。

 - §6.1 条件边路由 + §6.14 五类检索触发判据的**唯一实现点**（Q2 已决）。
 - 硬约束 §5.3#1：不引入 MCP，工具层就是这里的 if-else。
 - P1 只需重试边与 safety_hold 分流；五类判据随 P3（§13.2 P3）。
 - strict/loose 只调 confidence 阈值，不改判据、不改图（§6.8 / §6.11）。
"""
